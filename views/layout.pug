doctype
html(lang="is")
  head
    meta(charset="utf-8")
    link(rel="stylesheet" href="styles.css")
    title=title

  body

    if user
      div.menu
        div.menu-text
          p Innskráður notandi: #{user.name}
        div.menu-links  
          a(href="/") Forsíða
          a(href="admin") Stjórnsíða
          a(href="logout") Útskráning
    else
      div.menu
        div.menu-text
        div.menu-links
          a(href="/") Forsíða
          a(href="login") Innskráning
    block content

